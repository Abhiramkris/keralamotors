# Tasks - Remove GIFs and Video Loops from public Folder

- [x] Delete all generated `.gif` and `.mp4` files from the `/public` directory
- [x] Confirm files are removed from the filesystem
- [x] Run `npm run build` to confirm compilation
